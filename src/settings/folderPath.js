/**
 * File: folderPath.js
 * -----------------
 * Прописаны пути к необходимым файлам.
 */
import { join } from 'path';

export const APIS = join(`${__dirname}/../apis`);
export const INTERFACES = join(`${__dirname}/../interfaces`);
export const MODELS = join(`${__dirname}/../models`);
export const SERVICES = join(`${__dirname}/../services`);
export const LOG = join(`${__dirname}/../log`);
export const DB = join(`${__dirname}/../database`);
export const SEQUELIZE = join(`${__dirname}/../models`);
export const UTILS = join(`${__dirname}/../utils`);
